name: CI

on:
  pull_request:
  push:
    branches: [ main, master ]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test-sonar:
    name: Build • Test • Checkstyle • Coverage • Sonar (QG)
    runs-on: ubuntu-latest
    env:
      MAVEN_OPTS: -Xmx2g
      JAVA_TOOL_OPTIONS: -Dfile.encoding=UTF-8
      SONAR_HOST_URL: https://sonarcloud.io
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      CI: true

    # Ako ti je projekt u podfolderu, OTKOMENTARI i postavi ovde:
    # defaults:
    #   run:
    #     working-directory: TeamNest-Enterprise-Microservices-Demo

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Show mvnw perms (before)
        run: |
          pwd
          ls -la .
          if [ ! -f mvnw ]; then
            echo "mvnw not found in current dir. Searching..."
            find . -maxdepth 3 -name mvnw -type f
          fi

      - name: Fix mvnw perms (and CRLF just in case)
        run: |
          if [ -f mvnw ]; then
            sed -i 's/\r$//' mvnw
            chmod +x mvnw
            ls -la mvnw
          else
            echo "::error::mvnw not found at repo root. Set defaults.run.working-directory or adjust path."
            exit 1
          fi

      - name: Tool versions (wrapper)
        run: |
          java -version
          ./mvnw -v

      - name: Maven Verify (Checkstyle + Tests + IT + Coverage)
        run: ./mvnw -B -ntp -DskipITs=false verify

      - name: SonarCloud Analysis (wait for Quality Gate)
        if: ${{ env.SONAR_TOKEN != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false) }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./mvnw -B -ntp org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
            -Dsonar.host.url=${{ env.SONAR_HOST_URL }} \
            -Dsonar.qualitygate.wait=true

      - name: Skipped Sonar (no token or fork PR)
        if: ${{ !(env.SONAR_TOKEN != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)) }}
        run: echo "Skipping SonarCloud (no token or fork PR)."

      - name: Upload test & checkstyle reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            **/target/surefire-reports/**
            **/target/failsafe-reports/**
            **/target/checkstyle-result.xml
